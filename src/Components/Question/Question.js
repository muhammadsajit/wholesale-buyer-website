import React from 'react';

const Question = () => {
    return (
        <div>
            <div>
              <h1>How to react works</h1>
              <p>When we create a component in react.js then it show the actual Document Object Model in UI.Constiquently react create Virtual DOM and keeps all the component in it.When we change any component/state/data then react create new Virtual DOM.Then react compare the new Virtual DOM with the previous Virtual DOM.React do this for the check where data or state or component changes.By this way react Update the changes by diff Alogorithm and show the changes component in UI.</p>
            </div>
            <div>

            </div>
        </div>
    );
};

export default Question;